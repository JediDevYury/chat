directive @authenticated on FIELD_DEFINITION

scalar GraphQLISODateTime

"""
    User type
"""

type User {
    id: Int!
    fullName: String!
    email: String!
    createdAt: GraphQLISODateTime!
}

type AuthTokens {
    accessToken: String!
    refreshToken: String!
}

"""
    Inputs for the authentication
"""

input TokenInput {
    token: String!
}

input RefreshTokenInput {
    refreshToken: String!
}

"""
    Inputs for the users
"""

input UpdateUserInput {
    fullName: String
    email: String
}


"""
    Query types
"""
type Query {
    users: [User!]!
    user(id: ID!): User! @authenticated
}

"""
    Mutation types
"""

type Mutation {
    updateUser(id: ID!, updateUserInput: UpdateUserInput!): User!
    deleteUser(id: ID!): User

    authenticate(googleTokenInput: TokenInput!): User
    refreshTokens(refreshTokenInput: RefreshTokenInput!): AuthTokens
}
